# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.env

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts

# Local tooling / editor config — never published
.claude/
.vscode/
.idea/
CLAUDE.md
AGENTS.md
docs/superpowers/

# Audit ledger and its signing key (Phase 2) — runtime state, never committed
haven_ledger.db*
.audit_key

# Source documents for the corpus compiler (Phase 4) — redistributable only
# from their original publishers, so the manifest is tracked and the PDFs are not
corpus/sources/

# Editors / OS
.DS_Store
Thumbs.db
